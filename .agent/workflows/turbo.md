---
description: Allow auto-running all commands without user approval
---

// turbo-all

This workflow enables auto-running all safe git and npm commands without prompting for user approval every time.

Steps that can be auto-run:
1. git add, commit, push
2. npm install, npm run
3. File operations
4. Testing commands
